import mongoose from"mongoose";const connectDB=async()=>{try{const o=await mongoose.connect(process.env.MONGO_URI,{});console.log(`MongoDB Connected: ${o.connection.host}`)}catch(o){console.error(`Error: ${o.message}`),process.exit(1)}};export default connectDB;