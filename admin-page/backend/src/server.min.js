import express from"express";import cors from"cors";import dotenv from"dotenv";import connectDB from"../../../user-ticket/backend/config/database.js";import adminRoutes from"../routes/adminRoutes.js";dotenv.config();const app=express();app.use(cors()),app.use(express.json()),connectDB(),app.use("/api/admin",adminRoutes),app.use((o,s,e)=>{s.status(404).json({message:"Route not found"})}),app.listen(5001,()=>{console.log("===== Admin backend running on http://localhost:5001 =====")});